import EntityModelMock from '../../../../tests_config/EntityModelMock';

class PersonalInformation extends EntityModelMock {
  constructor(props: any) {
    super(props);
  }
}

export default PersonalInformation;
